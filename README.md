## Inicialização

Para executar o servidor local:

```bash
yarn dev
```

Para executar no modo build

```bash
yarn build
yarn start
```

Para rodar o cypress

```bash
yarn run cypress open

ou

yarn e2e
```
